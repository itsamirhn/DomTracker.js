{
	"name": "dom-tracker",
	"version": "0.0.0",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev --test-scheduled",
		"db:create": "wrangler d1 create dom-tracker-db",
		"db:migrate": "wrangler d1 migrations apply dom-tracker-db"
	},
	"devDependencies": {
		"wrangler": "^4.23.0"
	},
	"dependencies": {
		"@xmldom/xmldom": "^0.8.10",
		"xpath": "^0.0.33"
	}
}